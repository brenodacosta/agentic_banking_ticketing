apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: genai-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  # Rely on the cluster default StorageClass (typically gp2/gp3 on EKS)
