apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# NOTE: kind does not ship a default dynamic PV provisioner.
# Install one (e.g., rancher/local-path-provisioner) or adapt Postgres storage.
# This overlay intentionally does not change storageClassName; it is here to
# provide a clear entrypoint for future kind-specific patches.
